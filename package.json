{
  "name": "patternator",
  "version": "1.0.0",
  "private": true,
  "description": "Patternator — static seamless background pattern generator (200.work)",
  "scripts": {
    "patterns:generate": "node ./scripts/generate-pattern-manifest.mjs",
    "lint": "npm run lint:css && npm run lint:html",
    "lint:css": "stylelint \"assets/style/**/*.css\"",
    "lint:html": "htmlhint \"index.html\""
  },
  "devDependencies": {
    "htmlhint": "^1.9.2",
    "stylelint": "^17.13.0",
    "stylelint-config-standard": "^40.0.0"
  }
}
